You are a senior Python engineer updating two attached source files from an existing FastAPI GraphRAG gateway:

- core.py
- app.py

You are operating in a chatbot without a coding harness. You cannot edit the attachments in place, run tests, inspect other repository files, or use Git. Treat the attached files as the complete and authoritative originals.

Goal

Add document-name provenance to GraphRAG query responses. Preserve the original GraphRAG answer while mapping native `[Data: Sources (...)]` references to source-document filenames using existing parquet artifacts.

This must be implemented entirely in the two attached files. Do not require reindexing and do not add dependencies.

Scope rules

- Preserve all unrelated code, endpoint paths, operation IDs, request models, error handling, and behavior.
- Do not rewrite or simplify existing classes and functions.
- Do not modify `QueryCollectionRequest`.
- Do not invent repository APIs or structured GraphRAG output.
- Do not add caching, new environment variables, new endpoints, or new packages.
- `pandas` and `pyarrow` are already available.
- Always fail soft when provenance cannot be reconstructed. A query must still return its original answer.
- Produce syntactically complete Python files with no placeholders, omissions, or ellipses.

Required changes to core.py

Add these module constants near the existing query/index constants:

- `DATA_REFERENCE_RE`: case-insensitive regex matching the contents of `[Data: ...]`
- `SOURCES_SEGMENT_RE`: case-insensitive regex matching `Sources(...)`
- `NUMBER_RE`: regex matching integer source IDs
- `MAX_CITATION_SNIPPET_CHARS = 500`

Implement these exact functions:

1. `extract_source_reference_ids(answer: str) -> list[int]`

Read integer IDs only from `Sources(...)` segments inside `[Data: ...]` markers. Ignore IDs belonging to `Reports`, `Entities`, `Relationships`, and `Claims`. Deduplicate IDs while preserving first-seen order.

2. `citation_snippet(text: Any) -> str`

Convert the value to text, trim it, collapse all whitespace runs to one space, and truncate the result to 500 characters.

3. `load_source_citation_index(project_dir: Path) -> dict[int, dict[str, Any]]`

Read:

- `project_dir / "output" / "text_units.parquet"`
- `project_dir / "output" / "documents.parquet"`

Return `{}` if either file is missing, parquet loading fails, or required columns are absent.

Required text-unit columns:

- `human_readable_id`
- `id`
- `text`
- `document_id`

Required document columns:

- `human_readable_id`
- `id`
- `title`

Join `text_units.document_id` to `documents.id`, comparing them as strings. Use `text_units.human_readable_id` as the citation source ID.

Each indexed citation must have this exact shape:

{
    "source_id": int,
    "text_unit_id": str,
    "document_id": str,
    "document_source_id": int,
    "document": str,
    "snippet": str,
}

The `document` value must come from `documents.title`. Skip malformed or unmatched rows rather than raising an exception.

4. `build_query_citations(project_dir: Path, answer: str) -> list[dict[str, Any]]`

Return citations for source IDs referenced by the answer, preserving their first-seen order. Ignore referenced IDs that are not present in the citation index.

5. `documents_used_from_citations(citations: list[dict[str, Any]]) -> list[str]`

Return unique, non-empty document filenames in citation order.

6. `answer_with_document_citations(answer: str, citations: list[dict[str, Any]]) -> str`

For each `[Data: ...]` marker, inspect only its `Sources(...)` IDs. Append one annotation immediately after that marker:

` [Documents: filename-one; filename-two]`

Deduplicate filenames within each marker while preserving source order. If none of that marker's source IDs resolve to documents, leave the marker unchanged. Preserve every other character of the answer.

Required changes to app.py

Import these three helpers from `gateway.core`:

- `answer_with_document_citations`
- `build_query_citations`
- `documents_used_from_citations`

In the existing `graphrag_query_collection` endpoint:

1. Store `registry.project_dir(collection_id)` in `project_dir`.
2. Pass `project_dir` to the existing `runner.query(...)` call.
3. Build citations with `build_query_citations(project_dir, answer)`.
4. Preserve all existing response fields.
5. Keep the raw answer under `answer`.
6. Always add these response fields, including when citations are empty:

- `"answer_with_citations": answer_with_document_citations(answer, citations)`
- `"citations": citations`
- `"documents_used": documents_used_from_citations(citations)`

Acceptance checks

- Parsing `[Data: Sources (2, 7, 64, +more); Reports (1)]` returns `[2, 7, 64]`.
- Later duplicate source IDs are returned only once.
- Markers containing only Entities, Reports, Relationships, or Claims produce no source IDs.
- Missing or unreadable parquet files produce empty citations and leave the answer unchanged.
- If source 0 maps to `program_notes.txt`, then:

`Atlas manages Asterion [Data: Sources (0)].`

becomes:

`Atlas manages Asterion [Data: Sources (0)] [Documents: program_notes.txt].`

- The original `answer` field remains byte-for-byte unchanged.
- No indexing changes are required.

Output requirements

Silently review both completed files for missing imports, duplicate definitions, invalid type annotations, and accidental unrelated edits.

If your interface supports generated file attachments, return complete replacement files named `core.py` and `app.py`. Otherwise, return exactly two headings followed by the complete contents of each file in Python code fences.

Do not return only a diff. Do not abbreviate unchanged sections. Do not use `...`, “existing code remains unchanged,” or similar placeholders.

If both complete files cannot fit in one response, output the complete `core.py` first and end with exactly `READY_FOR_APP_PY`. When I reply `continue`, return the complete `app.py`.
